import { GetServerSideProps } from "next";
import { NextPage } from "next";
import { Layout } from "../components/Layout";

const Home: NextPage = () => {
  return (
    <Layout>
      <p>gola</p>
    </Layout>
  );
};

export default Home;
